# Copie para .env e ajuste. O .env não vai para o repositório.
# Todos têm default no docker-compose.yml, então o compose sobe sem .env nenhum.

POSTGRES_DB=jdice
POSTGRES_USER=jdice
POSTGRES_PASSWORD=jdice
POSTGRES_PORT=5432

API_PORT=5080
WEB_PORT=8080

ASPNETCORE_ENVIRONMENT=Production
CORS_ORIGIN=http://localhost:4200
